name: Continuous Integration

on:
  pull_request:
    branches: [main]
    paths:
      - "backend/**"
      - "frontend/**"
      - ".github/workflows/ci.yml"

permissions:
  contents: read

jobs:
  backend-test:
    name: Backend Test (Python)
    runs-on: ubuntu-latest
    env:
      GOOGLE_CLOUD_PROJECT: marketing-mail-generator
      FIREBASE_PROJECT_ID: marketing-mail-generator
    steps:
      - uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          python-version: "3.12"

      - name: Install dependencies
        working-directory: backend
        run: uv sync --frozen

      - name: Ruff lint
        working-directory: backend
        run: uv run ruff check .

      - name: Ruff format check
        working-directory: backend
        run: uv run ruff format --check .

      - name: Run Pytest (unit)
        working-directory: backend
        run: uv run pytest

  backend-integration:
    name: Backend Integration Test (Firestore Emulator)
    runs-on: ubuntu-latest
    env:
      # demo- プレフィックスはエミュレータ専用プロジェクトID（実プロジェクトへの誤接続を構造的に防ぐ）
      GOOGLE_CLOUD_PROJECT: demo-eventtune
      FIREBASE_PROJECT_ID: demo-eventtune
    steps:
      - uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          python-version: "3.12"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup Java (Firebase emulator)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Cache Firebase emulators
        uses: actions/cache@v4
        with:
          path: ~/.cache/firebase/emulators
          key: firebase-emulators-${{ runner.os }}

      - name: Install dependencies
        working-directory: backend
        run: uv sync --frozen

      - name: Run integration tests (emulator)
        run: >
          npx -y firebase-tools emulators:exec --only firestore,auth --project demo-eventtune
          "cd backend && uv run pytest -m integration"

  frontend-build:
    name: Frontend Build (Next.js)
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_API_URL: https://dummy-api.local
      NEXT_PUBLIC_FIREBASE_API_KEY: AIzaSyAyvwqPprZZick_OsyxnTfsStWAMb_rhhw
      NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: marketing-mail-generator.firebaseapp.com
      NEXT_PUBLIC_FIREBASE_PROJECT_ID: marketing-mail-generator
      NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: marketing-mail-generator.firebasestorage.app
      NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: "974233950009"
      NEXT_PUBLIC_FIREBASE_APP_ID: 1:974233950009:web:75bbffb7465a269bf8bae8
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Lint check
        working-directory: frontend
        run: npm run lint

      - name: Build project
        working-directory: frontend
        run: npm run build
