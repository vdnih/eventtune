# 現状の課題と直すべきポイント

本ドキュメントは 2026-07-02 時点のリポジトリレビューに基づく課題の棚卸しである。
今後の方針（[ROADMAP_PROPOSAL.md](ROADMAP_PROPOSAL.md)）の前提となる「足元の負債」を、
優先度付きで記録する。各課題には検証済みの根拠（ファイル・行）を付す。

優先度の定義: **P0** = 放置すると設計原則そのものが崩れる / **P1** = 訴求している価値と
実態の乖離 / **P2** = 条件付きリスク（トリガー監視で足りる）

---

## P0: 進行中作業（cost_items 移行）の完着

**現状**: ワーキングツリーに未コミットの変更が 7 ファイルある。内容は費用明細
（`CostItem`）を `events/{eid}/costs` サブコレクションから第3のファクト
`cost_items`（トップレベル）へ移行するもの（`ontology.py` に `CostCategory` / `CostItem` /
`CostSummary` 追加、`data_integration_agent.py` に決定論的 CSV 抽出パス追加、
Explorer / firestore.indexes.json の対応）。

**問題**: コード側は「5マスタ + **3ファクト**」に進んでいるが、正典・ドキュメントが
追従していない。

- 正典 YAML `backend/semantic/osi_event_marketing_v1.yml:131,138` — 旧パス
  `events/{event_id}/costs` を「実装済み」と記述したまま
- `docs/SEMANTIC_LAYER.md:194` — 同じく旧サブコレクションパスを記載
- `README.md` — ファクトは `EventAttendance` / `ProductInterest` の2つ前提の記述

**なぜ P0 か**: 「YAML が概念モデルの単一の思想源」（ADR-008）は本プロダクトの技術訴求の
根幹であり、正典が実装より古い状態は ADR-010 で是正したはずの乖離パターンの再発である。
ADR-010 自身が「大型変更の後は doc⇔impl 監査を1工程として設ける」と教訓化している。

**対処案**: この変更をコミットする前に、①正典 YAML に `cost_items` dataset を追加、
②SEMANTIC_LAYER / README の同期、③`_extract_cost_rows_from_csv` と `_build_cost_item` の
テスト追加（`test_ingestion_pipeline.py` に費用 CSV ケース）、④必要なら ADR 追記
（metrics 畳み込みから fact 昇格への方針転換は ADR-008 が「需要が出た時点で fact 化する」と
予告済みなので、その旨の追記で足りる）——までをワンセットで完了させる。

---

## P1: ドキュメント⇔実装の乖離（検証済み・具体箇所）

ADR-013 で `EngagementLevel` と取り込み時の業務判定は削除されたが、
`docs/PHILOSOPHY_AND_NAMING.md` に旧概念が残存している。

| 箇所 | 内容 | 実態 |
|---|---|---|
| PHILOSOPHY_AND_NAMING.md:107,131 | 「OntologyMapper が EngagementLevel 判定」「`_classify_engagement`」 | ADR-013 で削除済み（`ontology.py` に EngagementLevel は存在しない） |
| 同 :375,436-448 | Enum 一覧・Person 図に `engagement_level` | 同上 |
| 同 :593 | 「EngagementLevel に基づくブロック構成ルール」 | 同上 |
| 同 §5.3 チャットセッション | 「ADK の `InMemorySessionService` で管理」 | 実装は `VertexAiSessionService`（`marketing_agent.py:677`、ADR-009 判断2） |
| SOFTWARE_ARCHITECTURE.md マスタ一覧 | Person に「stage / engagement_level」と記載 | engagement_level は削除済み |

**なぜ問題か**: PHILOSOPHY_AND_NAMING は「システム設計の HOW の正典」を自認しており、
ここが古いと次の実装が撤回済み概念の上に積み上がる（ADR-010 が実際に経験した失敗モード）。
また、外部評価者がドキュメントを読んだとき、最も丁寧に書かれた文書に既知の誤りがあると
文書全体の信頼が割れる。

**対処案**: 半日規模の同期作業。PHILOSOPHY_AND_NAMING の該当節を ADR-013 準拠に改訂し
（観測事実 → `challenge_note` → appeal_vector 経路の説明に置換）、旧記述は §7 の移行メモへ
移す。あわせて `PM.md` は冒頭注記があるとはいえ実態と乖離が大きいため、
`docs/archive/` へ移動するか、現行ロードマップ（ROADMAP_PROPOSAL.md）への参照に置き換える。

---

## P1: 品質担保の欠落 — 訴求と検証のギャップ

### 1. 生成品質の eval が存在しない

ガードレール（捏造禁止・1機能1CEP・DBA 維持）は**プロンプトのソフト強制のみ**で、
これは ADR-007 が明示的に受け入れたトレードオフだが、その担保である HIL は「人が毎回
気づける」ことを前提にしている。プロンプト変更・モデル更新（現在 gemini-3.1-flash-lite）で
静かに劣化しても検知できない。**対処**: ROADMAP S-3 の eval ハーネスを最優先で導入し、
CI に組み込む。

### 2. HIL の承認がプロンプト規律に依存している

`run_assembly`（全件確定）は「明示承認必須」だが、それを強制するのはシステムプロンプトの
指示のみで、**構造的な強制（承認トークンなしでツールが実行を拒否する等）がない**。
テナント分離では「規律ではなく構造で」を貫いた本プロダクトの思想からすると、ここだけ
規律頼みなのは一貫性を欠く。**対処**: 確定系ツール（`run_assembly`、将来の送信系）に
サーバー側の承認フラグ（直前の承認イベントを検証してから実行）を導入する。

### 3. フロントエンドのテストが 0 件

バックエンドは `test_*.py` 4本＋CI があるが、フロントエンドにはテストがない。
特に `dashboard/page.tsx`（794行）は SSE イベント処理（`code` / `code_result` /
`tool_result`）という壊れやすいプロトコル解釈を含む。**対処**: SSE イベント →
UI 状態のパーサ部分を関数として切り出し、そこだけでもユニットテストを付ける
（全面的な E2E より費用対効果が高い）。

---

## P2: セキュリティ・運用（条件付きリスク）

### 1. アップロードファイル経由のプロンプトインジェクション

取り込みパイプラインはファイル内容（CSV セル・テキスト）を AI プロンプトに埋め込む
（`understand_batch` / `run_document_extractor` / 行単位抽出）。悪意ある CSV に
「これまでの指示を無視して…」が仕込まれた場合の影響評価がされていない。
**緩和要因**: AI ツールは closure 束縛で自スペース外に到達できず、取り込み AI の出力は
構造化スキーマ経由で決定論 Python が検証するため、被害半径は構造的に限定されている。
**対処**: 脅威として文書化した上で、抽出結果の異常値検知（例: appeal_summary への
指示文混入チェック）を eval に含める。マルチテナント SaaS を名乗る以上、
「評価済みで受容したリスク」と「未評価」の差は大きい。

### 2. Cloud Run BackgroundTask のスケールイン耐性

取り込み（`POST /api/integration/batches`）は BackgroundTask で実行される。Cloud Run の
インスタンス縮退・再起動でジョブが黙って死ぬと、`IntegrationJob` が進行中のまま残る。
**対処**: 当面はジョブのタイムアウト検知（一定時間更新のないジョブを failed 化）で足りる。
根本対応（Cloud Tasks）は ADR-002 の移行トリガー発火時でよい。

### 3. サンドボックス 14 日失効の UX

Agent Engine サンドボックスは 14 日無使用で消える（ADR-009 記載）。現状はエラーを返して
`get_space_data` 再実行を促す設計だが、この回復がユーザー体験としてどう見えるか
（チャットにエラーが出るのか、自動回復するのか）の確認・改善余地がある。
**対処**: `run_python_code` 内で失効を検知したら自動で再作成＋CSV 再投入して
リトライする（ユーザーに見せない）。

### 4. 総当たりコサインのスケール限界

設計上の意図的選択（ADR-008/010）であり現時点で問題ではないが、**再評価トリガー
（スペースあたり件数の閾値）が数値として定義されていない**。「大規模化したら」では
監視できない。**対処**: 例えば「1スペース 5,000 persons で計測し、分類レイテンシ p95 が
X 秒を超えたら近似最近傍へ」のように、トリガーを測定可能な形で ADR に追記する。

---

## P2: ビジネス面の未接続

- **効果測定ループが開いている**: 成果物は CSV エクスポートで終わり、開封・返信が
  戻らない。原則5（Historical Intelligence）の完成を妨げる最大の構造的ギャップであり、
  ポジショニング上も「蓄積するほど強い」という防御論理の証明を欠く
  （[POSITIONING.md](POSITIONING.md) §7、ROADMAP M-1 として提言済み）。
- **課金が未接続**: metering / plans は実装済みだが Stripe・プランゲーティングが未配線。
  クレジット超過時の挙動が未定義のため、現状は実質無制限（ROADMAP M-5）。
- **オンボーディングの前提知識**: 現体験は「何をチャットで頼めるか」をユーザーが
  知っている前提。初回スペース作成後の空状態で、サンプルデータ投入や指示例の提示が
  ないと、プロダクトの価値に到達する前に離脱しうる。デモでは問題にならないが、
  実利用では最初の壁になる。[CAPABILITY_MAP.md](CAPABILITY_MAP.md)（得意・不得意・
  頼み方）はこの穴を埋めるオンボーディング素材の原型として使える。

## P2: ポジショニング起点の課題（市場検討で顕在化）

[POSITIONING.md](POSITIONING.md) の市場分析から、プロダクト側に返る課題が3点見えた。

- **名前がカテゴリ誤認を招く**: リポジトリ名 `marketing-mail-generator` は「AI メール
  生成ツール」——市場で最もコモディティ化したカテゴリ——を自称する名前になっている。
  命名規約（PHILOSOPHY_AND_NAMING §6）が `generator` をシステム名に使うことを明示的に
  禁じているのに、リポジトリ名自体が違反しているのは皮肉な状態。プロダクト名の確立と
  リポジトリ名の変更（少なくとも README 冒頭での明確なポジション宣言）を検討すべき。
  → 名称候補と推奨案は [NAMING_PROPOSAL.md](NAMING_PROPOSAL.md) にまとめた。
- **差別化の核ほど露出が弱い**: 「AI がフォローメールを書く」は Cvent 等も到達済みで、
  もはや競争軸ではない。本プロダクトの差別化（取り込み根拠・自由分析・イベント横断
  蓄積）のうち、取り込み根拠は UI 未実装・分析はチャート未対応と、**見せ場側の実装が
  薄い**。訴求と露出の逆転を直す（ROADMAP S-1 / S-4 の優先度の裏付け）。
- **「向いていないこと」の線引きが利用者に見えない**: なんでも頼めるチャット UI は、
  Web リサーチや汎用ライティングなど本領外の依頼も受け付けてしまい、期待外れの体験を
  生みうる。CAPABILITY_MAP の内容をエージェントの応答（本領外の依頼への正直な案内）と
  オンボーディングに反映する実装が必要。

---

## 総括 — 直すべき順番

1. **cost_items 移行の完着**（P0）: 正典 YAML・ドキュメント・テストまで含めて 1 コミットで閉じる
2. **ドキュメント同期**（P1）: EngagementLevel 残存・セッションサービス記述の是正、PM.md の整理
3. **eval ハーネス**（P1）: 品質主張を検証可能にし、以降の変更の安全網とする
4. **HIL の構造的強制**（P1）: 確定系ツールにサーバー側承認検証を入れ、思想の一貫性を回復する
5. **P2 群**: トリガーの数値化と文書化を先に行い、実装はトリガー発火まで待つ

本プロダクトの強みは「規律ではなく構造」「記録された意思決定」にある。上記の課題は
いずれも致命傷ではなく、**その強みを自分自身に適用しきれていない箇所**の列挙である。
逆に言えば、直し方はすべて既存の設計原則の中に書いてある。
